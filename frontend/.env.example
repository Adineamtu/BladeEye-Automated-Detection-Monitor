# Absolute API origin used by frontend WebSocket worker.
# In local dev leave empty to use the current origin (recommended with Vite proxy).
VITE_API_BASE_URL=

# Vite dev-server proxy target for /api and /ws.
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
